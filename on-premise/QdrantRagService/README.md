# RAG System с Qdrant

Полноценная система Retrieval-Augmented Generation (RAG) с использованием Qdrant в качестве векторной базы данных и FastAPI для API.

## 🚀 Быстрый старт

### 1. Подготовка окружения

Убедитесь, что у вас установлены:
- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)

### 2. Клонирование и подготовка

```bash
# Создайте директорию проекта
mkdir rag-system
cd rag-system

# Создайте необходимые файлы (docker-compose.yml, Dockerfile, requirements.txt, main.py и др.)
# из предоставленных артефактов

# Создайте директории для данных
mkdir -p data qdrant_storage
```

### 3. Запуск системы

```bash
# Запуск через Docker Compose
docker-compose up -d

# Проверка статуса
docker-compose ps
```

Система будет доступна по адресам:
- **RAG API**: http://localhost:8000
- **Qdrant Web UI**: http://localhost:6333/dashboard
- **API Documentation**: http://localhost:8000/docs

## 📝 Использование

### Проверка работоспособности

```bash
# Проверка здоровья сервиса
curl http://localhost:8000/health

# Информация о коллекции
curl http://localhost:8000/collection/info
```

### Добавление данных

#### 1. Через API

```bash
# Добавление одного документа
curl -X POST "http://localhost:8000/add_document" \
     -H "Content-Type: application/json" \
     -d '{
       "text": "Ваш текст здесь",
       "metadata": {"category": "example"}
     }'

# Добавление нескольких документов
curl -X POST "http://localhost:8000/add_documents" \
     -H "Content-Type: application/json" \
     -d '[
       {
         "text": "Первый документ",
         "metadata": {"category": "doc1"}
       },
       {
         "text": "Второй документ", 
         "metadata": {"category": "doc2"}
       }
     ]'
```

#### 2. Через скрипт загрузки данных

```bash
# Запуск скрипта локально
python data_loader.py --sample  # Загрузка примеров данных

# Загрузка из JSON файла
python data_loader.py --file data/documents.json --type json

# Загрузка из CSV файла
python data_loader.py --file data/documents.csv --type csv --text-column "content"

# Загрузка из текстового файла
python data_loader.py --file data/document.txt --type txt --chunk-size 500
```

### Поиск контекста

#### Через API

```bash
# Поиск релевантных документов
curl -X POST "http://localhost:8000/search" \
     -H "Content-Type: application/json" \
     -d '{
       "query": "ваш поисковый запрос",
       "top_k": 5,
       "threshold": 0.7
     }'
```

#### Через тест-клиент

```bash
# Интерактивный режим поиска
python test_client.py --interactive

# Одиночный запрос
python test_client.py --query "ваш запрос"

# Комплексный тест системы
python test_client.py --test
```

## 🔧 API Endpoints

### Основные endpoints

- **GET** `/` - Проверка работы сервиса
- **GET** `/health` - Проверка здоровья системы
- **POST** `/search` - Поиск релевантных документов
- **POST** `/add_document` - Добавление одного документа
- **POST** `/add_documents` - Добавление нескольких документов
- **GET** `/collection/info` - Информация о коллекции
- **DELETE** `/collection/clear` - Очистка коллекции

### Примеры запросов

#### Поиск документов

```json
POST /search
{
  "query": "машинное обучение",
  "top_k": 3,
  "threshold": 0.5
}
```

Ответ:
```json
{
  "results": [
    {
      "text": "Машинное обучение — это метод анализа данных...",
      "score": 0.89,
      "metadata": {"category": "AI"}
    }
  ],
  "total_found": 1
}
```

## 🛠️ Разработка

### Локальный запуск без Docker

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск только Qdrant через Docker
docker run -p 6333:6333 qdrant/qdrant:latest

# Запуск сервиса
python main.py
```

### Настройка переменных окружения

```bash
export QDRANT_HOST=localhost
export QDRANT_PORT=6333
```

## 📊 Мониторинг

### Логи сервисов

```bash
# Просмотр логов RAG сервиса
docker-compose logs -f rag-service

# Просмотр логов Qdrant
docker-compose logs -f qdrant
```

### Qdrant Dashboard

Откройте http://localhost:6333/dashboard для доступа к веб-интерфейсу Qdrant.

## 🔍 Примеры данных

### Формат JSON

```json
[
  {
    "text": "Текст документа",
    "metadata": {
      "category": "AI",
      "author": "Author Name",
      "date": "2024-01-01"
    }
  }
]
```

### Формат CSV

```csv
text,category,author
"Текст первого документа",AI,Author1
"Текст второго документа",ML,Author2
```

## 🐛 Решение проблем

### Сервис не запускается

1. Проверьте, что порты 6333 и 8000 свободны
2. Убедитесь, что Docker работает
3. Проверьте логи: `docker-compose logs`

### Поиск не работает

1. Проверьте, что данные загружены: `curl http://localhost:8000/collection/info`
2. Попробуйте понизить threshold в запросе поиска
3. Убедитесь, что модель эмбеддингов загрузилась

### Медленная работа

1. Используйте batch-загрузку для большого количества документов
2. Настройте размер чанков для больших текстов
3. Рассмотрите использование GPU для ускорения эмбеддингов

## 📈 Масштабирование

### Для больших объемов данных

1. Увеличьте ресурсы контейнеров в docker-compose.yml
2. Настройте индексацию в Qdrant для улучшения производительности
3. Используйте разные коллекции для разных типов данных

### Продакшен деплой

1. Настройте reverse proxy (nginx)
2. Добавьте SSL сертификаты
3. Настройте мониторинг и логирование
4. Используйте внешнее хранилище для Qdrant

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения и протестируйте
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.