# Tree-Based Chat Versioning App

Приложение чата на React с Tailwind CSS, реализующее древовидное версионирование истории диалога. Каждое сообщение пользователя может иметь несколько альтернативных версий (веток), создавая разветвлённую структуру диалога.

## Ключевые особенности

- 🌳 **Древовидная структура диалога**
  - Сообщения пользователя - рёбра дерева
  - Ответы ассистента - узлы дерева
  - Поддержка множественных веток диалога

## Возможности

- ✅ Отправка и отображение сообщений
- ✅ Автоматическая прокрутка к последнему сообщению
- ✅ Адаптивный дизайн с Tailwind CSS
- ✅ **Эхо-бот** - автоматически отвечает тем же сообщением
- ✅ **Редактирование сообщений** - возможность редактировать свои сообщения
- ✅ Индикатор отредактированных сообщений
- ✅ **Автоматическое обновление ответов бота** - при редактировании сообщения пользователя
- ✅ **Навигация по версиям** - кнопки < 2/5 > для переключения между версиями сообщений
- ✅ **Полное версионирование** - сохранение всех версий с возможностью просмотра
- ✅ **Синхронная навигация** - все сообщения переключаются синхронно по версиям

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите приложение в режиме разработки:
```bash
npm start
```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Структура проекта

```
SDUChatBot/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   └── Chat.js         # Основной компонент чата
│   ├── App.js              # Главный компонент приложения
│   ├── index.js            # Точка входа
│   └── index.css           # Стили с Tailwind CSS
├── package.json
├── tailwind.config.js      # Конфигурация Tailwind
├── postcss.config.js       # Конфигурация PostCSS
└── README.md
```

## Структура данных дерева диалога

### ResponseNode (Узел - ответ ассистента)

```javascript
{
  id: number,              // Уникальный идентификатор узла
  parentEdgeId: number,    // ID родительского ребра (сообщения)
  text: string,            // Текст ответа ассистента
  createdAt: string,       // Время создания (ISO)
  childrenEdgeIds: [],     // Массив ID дочерних рёбер
  lastVisitedLeaf: number  // ID последнего посещённого листа в поддереве
}
```

### MessageEdge (Ребро - сообщение пользователя)

```javascript
{
  id: number,              // Уникальный идентификатор ребра
  fromNodeId: number,      // ID узла-источника
  toNodeId?: number,       // ID узла-назначения
  text: string,            // Текст сообщения
  kind: 'user',            // Тип ребра
  createdAt: string,       // Время создания (ISO)
  editOfEdgeId?: number    // ID оригинального ребра (при редактировании)
}
```

## Расширение функционала

Вы можете добавить свои функции версионирования и редактирования, используя следующие точки расширения:

1. **Состояние сообщений**: Массив `messages` в компоненте `Chat.js`
2. **Обработка отправки**: Функция `handleSendMessage`
3. **Отображение сообщений**: JSX разметка в методе `map` для отрисовки сообщений

### Идеи для реализации:

- ✅ ~~Добавить кнопку редактирования к каждому сообщению~~ (Реализовано)
- ✅ ~~Сохранить историю изменений для каждого сообщения~~ (Реализовано)
- ✅ ~~Добавить возможность отката к предыдущей версии~~ (Реализовано)
- Реализовать diff для отображения изменений
- ✅ ~~Добавить индикатор "отредактировано" к измененным сообщениям~~ (Реализовано)
- ✅ ~~Добавить версионирование сообщений с сохранением всех версий~~ (Реализовано)
- ✅ ~~Реализовать функцию просмотра истории изменений сообщения~~ (Реализовано)
- Добавить возможность удаления сообщений
- Реализовать функцию цитирования сообщений
- Добавить временные метки для каждой версии
- Реализовать экспорт истории версий

## Интеграция с внешними системами

Компонент Chat поддерживает callback функции для интеграции с внешними системами:

### Props компонента Chat:

```javascript
<Chat 
  onMessageEdit={handleMessageEdit}  // Вызывается при создании новой ветки
  onMessageSend={handleMessageSend}  // Вызывается при отправке сообщения
/>
```

### Структура данных при редактировании:

```javascript
const editData = {
  originalEdgeId: number,  // ID оригинального ребра
  newEdgeId: number,      // ID нового ребра (новой ветки)
  newText: string,        // Новый текст сообщения
  editedAt: string,       // Время редактирования (ISO)
};
```

### Пример использования:

```javascript
// В App.js уже есть базовый пример
const handleMessageEdit = (editData) => {
  // Сохранение в localStorage
  const editHistory = JSON.parse(localStorage.getItem('messageEditHistory') || '[]');
  editHistory.push(editData);
  localStorage.setItem('messageEditHistory', JSON.stringify(editHistory));
  
  // Или отправка на сервер
  // await api.saveMessageVersion(editData);
};
```

## Автоматическое обновление ответов бота

При редактировании сообщения пользователя, связанный ответ бота автоматически обновляется на новый текст. Это реализовано через связывание сообщений:

### Как это работает:

1. **Связывание сообщений**: При отправке сообщения создаются связи:
   - Сообщение пользователя получает `botReplyId`
   - Ответ бота получает `replyToId`

2. **Обновление при редактировании**: Когда пользователь редактирует своё сообщение:
   - Обновляется текст исходного сообщения
   - Автоматически обновляется текст связанного ответа бота
   - Оба сообщения помечаются как отредактированные

### Пример расширенного использования:

Смотрите файл `src/examples/VersioningExample.js` для примера:
- Полное версионирование с историей
- Сохранение всех версий сообщения
- Возможность отката к предыдущим версиям
- Отображение истории изменений
- Интеграция с localStorage и сервером

### Использование расширенного примера:

```javascript
// В src/App.js раскомментируйте импорт:
import VersioningExample from './examples/VersioningExample';

// И используйте компонент:
<VersioningExample />
```

## Навигация по веткам диалога

В древовидной структуре диалога возможно создание и переключение между разными ветками:

### Как это работает:

1. **Редактирование сообщения**
   - При редактировании создаётся новая ветка диалога
   - Сохраняется связь с оригинальным сообщением через `editOfEdgeId`
   - Автоматически создаётся новый ответ ассистента

2. **Переключение веток**
   - Для каждого узла с несколькими ветками отображаются кнопки переключения
   - При переключении обновляется весь последующий диалог
   - Сохраняется `lastVisitedLeaf` для быстрого возврата к последней позиции

3. **Отслеживание состояния**
   - Каждый узел хранит ID последнего посещённого листа в своём поддереве
   - При навигации автоматически обновляются все связи
   - Поддерживается возврат к началу диалога

### Ключевые методы компонента Chat:

```javascript
// Отправка нового сообщения
sendMessage(text) {
  // Создаёт новое ребро и эмулирует ответ ассистента
}

// Редактирование сообщения
editMessage(originalEdgeId, newText) {
  // Создаёт новую ветку на основе существующего сообщения
}

// Переключение между ветками
switchBranch(selectedEdgeId) {
  // Переходит к выбранной ветке диалога
}

// Получение текущего пути диалога
getConversationPath() {
  // Возвращает последовательность пар сообщение->ответ
}
```

### Преимущества древовидного версионирования:

1. **Сохранение контекста**: Каждая ветка сохраняет свой контекст диалога
2. **Параллельные версии**: Можно исследовать разные варианты развития диалога
3. **Гибкая навигация**: Легко переключаться между разными ветками
4. **История изменений**: Все варианты сообщений сохраняются в структуре
5. **Масштабируемость**: Дерево может расти в любом направлении

## Команды

- `npm start` - Запуск в режиме разработки
- `npm build` - Сборка для продакшена
- `npm test` - Запуск тестов

## Технологии

- React 18.2.0
- Tailwind CSS 3.4.1
- PostCSS
- Autoprefixer
